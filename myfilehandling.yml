---
- hosts: webservers
  become: yes
  tasks: 
   - include_tasks: myfile.yml
   
   - name: i want to install webserver on ubuntu
     apt:
       name: apache2
       state: present
     when: ansible_os_family =="Debian"  
